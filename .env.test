# Variáveis de ambiente para testes
NODE_ENV=test
JWT_SECRET=test-jwt-secret-for-testing
REGISTER_PASSWORD=admin123
FRONTEND_URL=http://localhost:3000

# MongoDB será configurado automaticamente pelo mongodb-memory-server
# MONGODB_URI será sobrescrito pelo setup dos testes
